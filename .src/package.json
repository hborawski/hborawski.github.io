{
  "name": "dominion-site",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "export": "rm -r ../* && next export && mv out/* .. && rm -rf out && touch ../_next/.nojekyll",
    "start": "next start"
  },
  "dependencies": {
    "next": "^9.1.7",
    "next-images": "^1.3.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "styled-components": "^4.4.1"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.10.6"
  }
}
